<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <!-- Bundled into the DtoOrm package via DtoOrm.MariaDb — not a standalone NuGet package. -->
    <IsPackable>false</IsPackable>
  </PropertyGroup>
</Project>
